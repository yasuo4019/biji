
`head` 命令用于显示文件的开头部分，默认情况下显示文件的前10行。

## 命令格式
```bash
head [选项] [文件]
```

## 常用选项

- `-n num` 或 `--lines=num`：显示文件的前 num 行，默认为 10 行。
- `-c num` 或 `--bytes=num`：显示文件的前 num 个字节。

## 示例

1. 显示文件的前 5 行：
   ```bash
head -n 5 file.txt
   ```

2. 显示文件的前 20 个字节：
   ```bash
head -c 20 file.txt
   ```
